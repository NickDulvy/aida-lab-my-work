# Notebook kernel

**Don't fight Cursor's kernel picker.** Double-click **Run Notebook.command** in the project root instead. It opens Jupyter Lab in your browser with the correct environment — no kernel selection needed.

See **NOTEBOOK-SETUP.md** in the project root.
